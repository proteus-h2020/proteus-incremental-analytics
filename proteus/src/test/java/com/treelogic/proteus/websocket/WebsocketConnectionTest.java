package com.treelogic.proteus.websocket;

import org.junit.Test;
import static org.junit.Assert.*;

public class WebsocketConnectionTest {
	
	
	
	@Test
	public void test(){
		assertTrue(1 == 1);
	}
	/**
	@Test
	public void startAndStopServer() throws Exception{
		JettyServer webserver = new JettyServer(8025);
		webserver.start();
		boolean isStarted = webserver.isStarted();
		assertTrue(isStarted);
		webserver.stop();
		boolean isStopped = webserver.isStopped();
		assertTrue(isStopped);
	}
	**/
	
	
}
